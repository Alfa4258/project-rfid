name: project_rfid
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  flutter:
    sdk: flutter
    
  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_native_splash: ^2.2.20
  file_picker: ^5.0.0
  http: ^1.2.2
  serial_communication: ^0.0.2
  flutter_libserialport: ^0.4.0
  sqflite: ^2.0.0+3  # The latest version of sqflite
  sqflite_common_ffi: ^2.0.0  # Add this for desktop support
  excel: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/regulus.png
    - assets/background.jpg
    - assets/marathon.jpg
